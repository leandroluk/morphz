{
  "name": "@morphz/ts-plugin-internal",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript Language Service plugin for morphz — bundled into morphz's own dist/ts-plugin subpath, never published standalone.",
  "main": "./dist/index.cjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "morphz": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.3.0",
    "@typescript/vfs": "^1.6.4",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  },
  "peerDependencies": {
    "typescript": "^5.6.0"
  }
}
